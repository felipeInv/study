Algoritmo "Contar com para"
   var
   cond:inteiro
   Inicio
         //(para) funcção (cond) variável de controle (<- 1) recebe o valor 1 (ate 10) ele vai repitir até a variável de controle valer 10
         para cond <- 1 ate 10 faca
         escreva(cond,"...")
         fimpara
Fimalgoritmo