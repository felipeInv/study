function falarOi(){
    return('oi');
};

const vaiExecutar = false;

// console.log(vaiExecutar && falarOi());

console.log(0 || false || null || 'Luiz Otávio' || true);
let corUser = 'verde';
let corDefault = 'vermelho';

console.log(corUser || corDefault)